<%# Uses rails_bootstrap_form gem.  i.e.:

  bootstrap_form_for(workflow) do |f|

API at: https://github.com/bootstrap-ruby/rails-bootstrap-forms %>

<%= f.select :queue_name, [["entry"], ["long"]], { selected: "entry", label: "Queue" }, { class: "selectpicker" } %>
<%= f.number_field :num_nodes, { value: 1, label: "Number of nodes" } %>
<%= f.number_field :cpu_cores, { value: 1, label: "Number of CPU cores" } %>
<%= f.number_field :memory, { value: 1, label: "Amount of memory", help: "Value is in Gb" } %>
